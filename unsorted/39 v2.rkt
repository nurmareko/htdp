;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname |39 v2|) (read-case-sensitive #t) (teachpacks ((lib "image.rkt" "teachpack" "2htdp") (lib "universe.rkt" "teachpack" "2htdp"))) (htdp-settings #(#t constructor repeating-decimal #f #t none #f ((lib "image.rkt" "teachpack" "2htdp") (lib "universe.rkt" "teachpack" "2htdp")) #f)))
;		Physical constants

; the radius of the wheel
; used as a single point of control.
(define WHEEL-RADIUS ...)

; the y position of the car image
; to the background.
(define Y-CAR ...)

; the x position of the tree image
; to the background.
(define X-TREE ...)

; the x position of the tree image
; to the background.
(define Y-TREE ...)

; the color of the car image.
(define CAR-COLOR ...)

; the width of the background.
(define BG-WIDTH ...)

; the height of th background.
(definje BG-HEIGHT ...)


;		Graphical constants

(define WHEEL ...)
(define LOWER-BODY ...)  
(define MIDDLE-BODY ...)
(define UPPER-BODY ...)
(define BUMPER ...)
(define WINDOW ...)
(define BONNET ...)
(define CAR ...)
(define BACKGROUND ...)
(define TREE ...)